# Local Companion Package Workspace

This folder is a local workspace for companion packages that are intentionally
kept out of the published `searchlight` repo history.

Planned local package folders:

- `.companions/searchlight_parsedoc`
- `.companions/searchlight_highlight`
- `.companions/searchlight_pdf`

Only this note is tracked. Package contents stay gitignored so they can be
iterated on here and moved into separate repos later.
